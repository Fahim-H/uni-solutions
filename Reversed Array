// Write a program thatthat reads 10 integers and displays 
//them in the reverse of  the order in which they were read.

#include <iostream>
#include <iomanip>
#include <stdio.h>
using namespace std;

int main()
{
    int nums[10];

    for(int i=0;i<10;i++)cin>>nums[i];
    for(int i=9;i>=0;i--)cout<<nums[i]<<" ";
    cout<<endl;
    return 0;
}
